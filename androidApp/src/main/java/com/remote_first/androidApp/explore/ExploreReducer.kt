package com.remote_first.androidApp.explore

import com.remote_first.shared.flow_redux.Reducer
import javax.inject.Inject

class ExploreReducer @Inject constructor() : Reducer<ExploreState, ExploreResult> {
    override fun reduce(state: ExploreState, result: ExploreResult): ExploreState {
        TODO("Not yet implemented")
    }
}
